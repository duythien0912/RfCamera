name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '4.0.0-beta.1'
      SERVERPOD_PASSWORD_database: 'oFmnMkh9s2YEnncHQnrWrMWNOnV_MUmC'
      SERVERPOD_PASSWORD_redis: 'rd_kg5Dfm-ddA5QSIPJUTaxGGoSAqjO7'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.4'

      - name: Start containers
        working-directory: rfcam_server
        run: docker compose up --quiet-pull --build -d

      - name: Install dependencies
        run: flutter pub get

      - name: Install Serverpod CLI
        run: dart install serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: rfcam_server
        run: serverpod generate

      - name: Run tests
        working-directory: rfcam_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: rfcam_server
        run: docker compose down -v
